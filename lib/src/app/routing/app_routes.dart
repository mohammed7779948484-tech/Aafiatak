/// Route paths owned by the application shell.
///
/// Product feature routes should be added deliberately as their patient flows
/// are implemented from the approved screen-flow documentation.
abstract final class AppRoutes {
  const AppRoutes._();

  static const String root = '/';
}
